# Codex 작업 지침

## 적용 범위

이 지침은 eunseong4205-boop/eunseong 저장소 전체에 적용합니다.

## 프로그램 개발과 자동 검사

- 프로그램을 새로 만들거나 기존 프로그램을 변경할 때, 실제 언어·프레임워크·패키지 관리자에 맞는 테스트와 빌드 명령을 함께 설정하고 유지합니다.
- 핵심 기능과 변경된 동작을 검증하는 의미 있는 자동 테스트를 작성합니다. 테스트가 없는데 성공으로 처리하는 대체 명령을 사용하지 않습니다.
- 실행 가능한 빌드 과정이 있는 프로젝트에는 실제 빌드 명령을 추가합니다. 별도 빌드가 없는 프로젝트는 해당 사실을 README와 결과 보고에 명시하고 적합한 실행·구문 검사를 구성합니다.
- .github/workflows/ 아래 GitHub Actions를 함께 갱신하여 main push와 PR 생성·커밋 업데이트·다시 열기 시 실제 테스트 및 빌드가 자동 실행되게 합니다. 현재 기본 검사만으로 앱 테스트나 빌드가 완료됐다고 보고하지 않습니다.
- 런타임 버전과 의존성 설치 방법을 프로젝트 설정에 맞추고, 잠금 파일이 있으면 재현 가능한 설치 명령을 사용합니다.
- 테스트나 빌드 실패를 무시하거나 숨기지 않습니다. 원인을 수정하고 관련 검사를 다시 실행합니다.
- 공개 저장소에서 무료로 사용할 수 있는 표준 GitHub-hosted 러너를 기본으로 사용합니다. CI에 유료 AI API 호출을 추가하지 않습니다.
- GitHub에 변경 사항을 반영한 경우 해당 커밋의 Actions 결과를 확인하고 실행 링크와 성공·실패·미검증 상태를 정확히 보고합니다. 반영하지 않은 작업은 로컬 검증과 원격 CI 미실행을 구분합니다.
- 테스트·빌드·로컬 실행 방법을 README에 문서화합니다.

## 현재 상태

현재는 프로그램 코드가 없어 공백 오류와 병합 충돌 표시 검사만 있습니다.
첫 프로그램을 추가할 때 위 지침에 따라 실제 테스트·빌드를 함께 구성해야 합니다.
